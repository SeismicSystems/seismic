{
  "name": "seismic-viem-test-runner",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rimraf dist",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "viem": "2.x",
    "seismic-viem": "workspace:*",
    "seismic-viem-tests": "workspace:*"
  }
}
