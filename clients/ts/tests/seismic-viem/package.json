{
  "name": "seismic-viem-test-runner",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rimraf dist",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "test:unit": "bun test src/viem-unit.test.ts"
  },
  "devDependencies": {
    "viem": "2.x",
    "seismic-viem": "workspace:*",
    "seismic-viem-tests": "workspace:*"
  }
}
